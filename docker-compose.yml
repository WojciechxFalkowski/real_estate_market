version: "3"
services:
  vue-front:
    build:
      context: .
      args:
        - PORT=${PORT}
    ports:
      - ${PORT}:${PORT}
  nginx:
    build: ./nginx
    restart: always
    ports:
      - '80:80'
    depends_on:
      - vue-front